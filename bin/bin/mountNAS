#!/bin/bash
mkdir -p $HOME/Fisiere
sudo mount -t cifs //vsynnas.local/Fisiere $HOME/Fisiere -o credentials=$HOME/creds.txt,rw,vers=2.0,uid=1000
#sudo mount -t nfs -o rw,uid=1000 192.168.1.120:/volume1/Fisiere $HOME/Fisiere
